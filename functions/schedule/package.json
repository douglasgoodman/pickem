{
    "name": "@pickem/schedule-function",
    "packageManager": "yarn@3.2.3",
    "dependencies": {
        "aws-sdk": "^2.1222.0",
        "axios": "^0.27.2"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.104",
        "@types/node": "^18.7.18",
        "esbuild": "^0.15.8",
        "nodemon": "^2.0.20",
        "ts-node": "^10.9.1",
        "typescript": "^4.8.3"
    },
    "scripts": {
        "build": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
        "build:image": "yarn docker build",
        "package": "cd dist && 7z a index.zip *",
        "typecheck": "tsc"
    }
}
