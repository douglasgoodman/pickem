{
    "name": "@pickem/schedule-function",
    "packageManager": "yarn@3.2.3",
    "dependencies": {
        "axios": "^0.27.2"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.104",
        "@types/node": "^18.7.18",
        "esbuild": "^0.15.8",
        "nodemon": "^2.0.20",
        "ts-node": "^10.9.1",
        "typescript": "^4.8.3"
    },
    "scripts": {
        "build": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=node16 --outfile=dist/index.js",
        "build:image": "yarn docker build",
        "typecheck": "tsc"
    }
}
